name: Deploy Frontend

on:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/deploy-frontend.yml"
      - "k8s/helm/**"
      - "k8s/values-frontend.yaml"
      - "services/hackathon-starter-frontend/**"
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

concurrency:
  group: deploy-frontend-${{ github.ref }}
  cancel-in-progress: true

env:
  AWS_REGION: ${{ vars.AWS_REGION || 'ap-southeast-1' }}
  EKS_CLUSTER_NAME: ${{ vars.EKS_CLUSTER_NAME }}
  K8S_NAMESPACE: ${{ vars.K8S_NAMESPACE || 'app' }}
  HELM_RELEASE_NAME: frontend
  IMAGE_REPOSITORY: ${{ secrets.DOCKERHUB_USERNAME }}/hackathon-starter-frontend
  IMAGE_TAG: ${{ github.sha }}

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24.x
          cache: npm
          cache-dependency-path: ./services/hackathon-starter-frontend/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: ./services/hackathon-starter-frontend
        env:
          HUSKY: 0

      - name: Run lint check
        run: npm run lint-check
        working-directory: ./services/hackathon-starter-frontend

      - name: Run tests
        run: npm run test
        working-directory: ./services/hackathon-starter-frontend

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: ./services/hackathon-starter-frontend
          file: ./services/hackathon-starter-frontend/Dockerfile
          push: true
          tags: |
            ${{ env.IMAGE_REPOSITORY }}:${{ env.IMAGE_TAG }}
            ${{ env.IMAGE_REPOSITORY }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Run Trivy scan
        uses: aquasecurity/trivy-action@latest
        with:
          image-ref: ${{ env.IMAGE_REPOSITORY }}:${{ env.IMAGE_TAG }}
          format: table
          exit-code: 0 # Don't fail the build on vulnerabilities, but report them.
          ignore-unfixed: true
          severity: CRITICAL

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_TO_ASSUME }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Update kubeconfig for EKS
        run: aws eks update-kubeconfig --name "$EKS_CLUSTER_NAME" --region "$AWS_REGION"

      - name: Set up Helm
        uses: azure/setup-helm@v4

      - name: Deploy to EKS
        run: |
          helm upgrade --install "$HELM_RELEASE_NAME" ./k8s/helm \
            --namespace "$K8S_NAMESPACE" \
            --create-namespace \
            -f ./k8s/values-frontend.yaml \
            --set image.repository="$IMAGE_REPOSITORY" \
            --set image.tag="$IMAGE_TAG"
