apiVersion: v1
kind: Service
metadata:
  name: {{ include "hackathon-starter.fullname" . }}
  labels:
    {{- include "hackathon-starter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hackathon-starter.selectorLabels" . | nindent 4 }}
