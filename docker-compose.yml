
services:
  mongo:
    image: mongo:latest
    ports:
      - '27017:27017'
  backend:
    # build: services/hackathon-starter-backend
    image: vvi11iamng0/hackathon-starter-backend@sha256:4aea6044185a82dabce14d90244e635bdf3d47315fe4aa6b495d058a96340999
    ports:
      - '8080:8080'
    environment:
      - PORT=8080
      - BASE_URL=http://backend:8080
      - MONGODB_URI=mongodb://mongo:27017/test
    links:
      - mongo
    depends_on:
      - mongo
  frontend:
    # build: services/hackathon-starter-frontend
    image: vvi11iamng0/hackathon-starter-frontend
    ports:
      - '3000:3000'
    environment:
      - PORT=3000
      - BASE_URL=http://localhost:3000
      - BACKEND_URL=http://backend:8080
      - MONGODB_URI=mongodb://mongo:27017/test
    links:
      - mongo
      - backend
    depends_on:
      - backend
